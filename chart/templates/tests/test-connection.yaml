apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodepools-label.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "nodepools-label.name" . }}
    helm.sh/chart: {{ include "nodepools-label.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nodepools-label.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
